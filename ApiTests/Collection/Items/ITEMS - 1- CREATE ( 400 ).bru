meta {
  name: ITEMS - 1: CREATE ( 400 )
  type: http
  seq: 3
}

post {
  url: {{base_url}}/api/v1/users/adinan/collection/items
  body: none
  auth: inherit
}

assert {
  res.status: eq 400
}

settings {
  encodeUrl: true
  timeout: 0
}
