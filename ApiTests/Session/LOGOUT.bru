meta {
  name: LOGOUT
  type: http
  seq: 4
}

get {
  url: {{base_url}}/api/v1/session
  body: none
  auth: inherit
}

script:post-response {
  bru.setVar('session', '');
  
}

settings {
  encodeUrl: true
  timeout: 0
}

docs {
  Closes the current session.
  
}
