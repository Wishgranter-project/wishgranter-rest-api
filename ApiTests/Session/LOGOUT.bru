meta {
  name: LOGOUT
  type: http
  seq: 4
}

delete {
  url: {{base_url}}/api/v1/session
  body: none
  auth: inherit
}

headers {
  Cookie: session={{session}}
}

script:post-response {
  bru.setVar('session', '');
  
}

tests {
  
  
}

settings {
  encodeUrl: true
  timeout: 0
}

docs {
  Closes the current session.
  
}
