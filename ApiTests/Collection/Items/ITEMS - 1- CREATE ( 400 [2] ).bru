meta {
  name: ITEMS - 1: CREATE ( 400 [2] )
  type: http
  seq: 4
}

post {
  url: {{base_url}}/api/v1/users/adinan/collection/items
  body: multipartForm
  auth: inherit
}

body:multipart-form {
  title: music title
  playlist: music-metal
  not-a-thing: this is
}

assert {
  res.status: eq 400
}

settings {
  encodeUrl: true
  timeout: 0
}
