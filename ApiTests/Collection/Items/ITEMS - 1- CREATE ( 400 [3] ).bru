meta {
  name: ITEMS - 1: CREATE ( 400 [3] )
  type: http
  seq: 5
}

post {
  url: {{base_url}}/api/v1/users/adinan/collection/items
  body: multipartForm
  auth: inherit
}

body:multipart-form {
  title: music title
  playlist: non-existent
}

assert {
  res.status: eq 400
}

settings {
  encodeUrl: true
  timeout: 0
}
