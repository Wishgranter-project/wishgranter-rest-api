meta {
  name: PLAYLISTS - 1: CREATE ( 400[2] )
  type: http
  seq: 3
}

post {
  url: {{base_url}}/api/v1/users/adinan/collection/playlists
  body: multipartForm
  auth: inherit
}

body:multipart-form {
  title: test playlist
  random_property: nonsense
}

assert {
  res.status: eq 400
}

settings {
  encodeUrl: true
  timeout: 0
}
