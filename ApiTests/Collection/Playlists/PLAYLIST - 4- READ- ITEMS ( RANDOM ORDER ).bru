meta {
  name: PLAYLIST - 4: READ: ITEMS ( RANDOM ORDER )
  type: http
  seq: 12
}

get {
  url: {{base_url}}/api/v1/users/adinan/collection/playlists/music-metal/items?orderBy=RAND()
  body: none
  auth: inherit
}

params:query {
  orderBy: RAND()
}

assert {
  res.status: eq 200
}

settings {
  encodeUrl: true
  timeout: 0
}
