meta {
  name: ITEMS - 3: READ: SEARCH ( RANDOM ORDER )
  type: http
  seq: 9
}

get {
  url: {{base_url}}/api/v1/users/adinan/collection/items?genre=power metal&orderBy=RAND()
  body: none
  auth: inherit
}

params:query {
  genre: power metal
  orderBy: RAND()
}

assert {
  res.status: eq 200
}

settings {
  encodeUrl: true
  timeout: 0
}
